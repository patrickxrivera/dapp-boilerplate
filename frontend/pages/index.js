import { useEffect } from "react";
import ethers from "../setup/ethers";

const Home = () => {
    useEffect(() => {
        const greet = async () => {
            // const  greeting = await ethers
        };

        greet();
    });

    return <h1>Hello</h1>;
};

export default Home;
